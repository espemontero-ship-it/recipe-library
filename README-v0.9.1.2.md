# Recipe Library v0.9.1.2

Fixes:
- Detects markdown-formatted servings such as `**Servings:** 6`.
- Also accepts `Serves`, `Makes`, `Yield`, and `Raciones`.
- Adds `Add recipe` to the main navigation for the authenticated administrator only.
