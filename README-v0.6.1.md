# Recipe Library v0.6.1

- Reads the model v2 fields from Supabase.
- Search includes ingredients_index, dish, format, meal_type, cuisine and cooking method.
- Uses source_type and cover_image.
- Keeps legacy aliases temporarily so the current paste importer still works.
