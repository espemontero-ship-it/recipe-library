Recipe Library v0.9.0.2 — Magic link login

Copy the app and lib folders over the project root.
Then delete .next and run npm run dev.

Supabase Auth URL configuration for local use:
Site URL: http://localhost:3000
Redirect URL: http://localhost:3000/**
