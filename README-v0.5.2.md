# Recipe Library v0.5.2

- Multi-select ingredient and method filters
- High protein filter
- Under 500 kcal filter
- Active-filter chips
- Clear all filters

Install: copy the `app` folder over the project and replace files.

Tested with `npm run build`.
