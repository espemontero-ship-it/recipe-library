import Link from "next/link"; import type { Recipe } from "@/lib/types"; export function RecipeCard({recipe}:{recipe:Recipe}){return <Link href={`/recipes/${recipe.id}`} className="recipe-card"><div className="card-image"><span>{recipe.mainIngredients[0]}</span></div><div className="card-copy"><p className="eyebrow">{recipe.status}</p><h3>{recipe.title}</h3><p className="meta">{recipe.author} · {recipe.publication}</p><p className="nutrition-line">{recipe.calories} kcal · {recipe.protein} protein</p></div></Link>}