import { AppHeader } from "@/components/AppHeader"; import { RecipeCard } from "@/components/RecipeCard"; import { recipes } from "@/lib/recipes"; export default function BrowsePage(){return <><AppHeader/><main className="page-shell"><div className="browse-header"><div><p className="eyebrow">BROWSE</p><h1>Find your next recipe.</h1></div><input className="search-input" placeholder="Search chicken, Sue Li, miso..."/></div><div className="browse-layout"><aside className="filters"><h2>Filters</h2>{["Chicken","Shrimp","Salad","Air Fryer","NYT Cooking","This Weekend"].map(x=><button key={x}>{x}</button>)}</aside><section className="recipe-grid">{recipes.map(r=><RecipeCard key={r.id} recipe={r}/>)}</section></div></main></>}