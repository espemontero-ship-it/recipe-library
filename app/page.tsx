import { AppHeader } from "@/components/AppHeader"; import { RecipeCard } from "@/components/RecipeCard"; import { recipes } from "@/lib/recipes"; export default function HomePage(){const weekend=recipes.filter(r=>r.status==="This Weekend");const favorites=recipes.filter(r=>r.status==="Favorite");return <><AppHeader/><main className="page-shell"><section className="hero"><p className="eyebrow">YOUR PERSONAL COOKBOOK</p><h1>Recipes worth cooking again.</h1><p>Browse, cook and share the recipes you actually care about.</p><a href="/browse" className="button">Browse recipes</a></section><Section title="This Weekend" recipes={weekend}/><Section title="Favorites" recipes={favorites}/><Section title="Recently Added" recipes={recipes}/></main></>} function Section({title,recipes}:{title:string;recipes:typeof import("@/lib/recipes").recipes}){return <section className="content-section"><div className="section-heading"><h2>{title}</h2><a href="/browse">See all</a></div><div className="recipe-grid">{recipes.map(r=><RecipeCard key={r.id} recipe={r}/>)}</div></section>}